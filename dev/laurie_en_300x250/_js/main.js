import {init, init2} from '../../_common/js/common.js'

const data = {
	name: "laurie",
	tween: {x:-130, y:-125}
	// tween: {x:-250, y:-151}
}

const tl = init2(data)

// @include pos-scale(-131, -125, .5);